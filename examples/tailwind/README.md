# Tailwind Example

Tailwind is an example styling choice, not a package requirement.

```tsx
<DateInput.Root calchemy={calchemy}>
  <DateInput.Field className="rounded-md border px-3 py-2" />
  <DateInput.Candidates className="mt-2 grid gap-1" />
  <DateInput.Calendar className="mt-2 grid grid-cols-7 gap-1" />
</DateInput.Root>
```
