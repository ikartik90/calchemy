{
  "name": "@calchemy/date-react",
  "version": "0.0.0",
  "type": "module",
  "description": "Headless React primitives for Calchemy date input.",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --sourcemap --tsconfig tsconfig.build.json --external react --external react/jsx-runtime --external @calchemy/date-core && tsc -p tsconfig.build.json --emitDeclarationOnly",
    "test": "vitest run --root ../.. --config vitest.config.ts packages/date-react/tests --environment jsdom",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@calchemy/date-core": "workspace:*"
  },
  "peerDependencies": {
    "react": ">=18.3.0 || >=19.0.0",
    "react-dom": ">=18.3.0 || >=19.0.0"
  },
  "devDependencies": {}
}
