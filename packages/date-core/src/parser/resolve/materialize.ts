import type { DateValue } from "../../types";

// Example: `materializeDateValue(value)` returns the final DateValue emitted by the resolver pipeline.
export function materializeDateValue(value: DateValue): DateValue {
  return value;
}
