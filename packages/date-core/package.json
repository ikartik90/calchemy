{
  "name": "@calchemy/date-core",
  "version": "0.3.0",
  "type": "module",
  "description": "Natural language date parser with Temporal values.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ikartik90/calchemy.git"
  },
  "homepage": "https://github.com/ikartik90/calchemy#readme",
  "bugs": "https://github.com/ikartik90/calchemy/issues",
  "keywords": [
    "date",
    "parser",
    "temporal",
    "natural-language"
  ],
  "publishConfig": {
    "access": "public"
  },
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --sourcemap --clean --tsconfig tsconfig.build.json --external @js-temporal/polyfill && tsc -p tsconfig.build.json --emitDeclarationOnly",
    "test": "pnpm run build && vitest run --root ../.. --config vitest.config.ts packages/date-core/tests",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@js-temporal/polyfill": "^0.5.1"
  }
}
